<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Mongo Scraper</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/">Home</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="/savedArticles">Saved articles<span class="sr-only">(current)</span></a>
      </li>
      <form class="form-inline">
        <a id="clear" class="btn btn-outline-success" role="button" href="/clearsaved">Clear articles</a>
      </form>
    </ul>
  </div>
</nav>

<div class="jumbotron jumbotron-fluid shadow-sm" id="saved-background">
  <div class="container">
    <h1 class="display-2 text-center py-5 text-white">Saved articles</h1>
    <p class="lead text-center text-white">Your saved articles can be found here.</p>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      {{#each articles}}
      <div class="card shadow mb-3">
        <div class="card-header bg-transparent">
          <a id="linkinput" href="https://www.teenvogue.com/{{link}}" target="blank">
            <h5 id="titleinput" class="card-title">{{title}}</h2>
          </a>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-8">
              <p id="summaryinput" class="card-text">{{summary}}</p>
            </div>
            <div class="col-md-4">
              <button class="btn btn-outline-danger mx-2 deleteArticle" data-id="{{this.id}}">Delete from saved</button>
              <button type="button" class="btn btn-outline-success addNote" data-toggle="modal" data-target="#noteModal"
                data-id="{{this.id}}">Add Note</button>
            </div>
          </div>
        </div>
      </div>

      <!-- Modal -->
      <div class="modal fade" id="noteModal" tabindex="-1" role="dialog" aria-labelledby="noteModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="noteModalLabel">{{title}}</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              <div class="card mb-2">
                <div class="card-body">
                  <p id="noteBody"></p>
                  <button class="btn btn-danger mx-2 deleteNote" data-id="{{this.id}}">Delete note</button>
                </div>
              </div>

              <form>
                <div class="form-group">
                  <label for="message-text" class="col-form-label">New Note:</label>
                  <textarea class="form-control" id="newNote"></textarea>
                </div>
              </form>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <button type="button" class="btn btn-primary" data-id="{{this.id}}" id="saveNote">Save note</button>
            </div>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</div>